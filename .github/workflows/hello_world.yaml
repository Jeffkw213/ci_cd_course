name: Hello World
on:
    workflow_dispatch:
    push:
        branches:
            - main
            - feature/*
jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        name: Hello World Job
        steps:
            - name: Hello World
              run: echo "Hello World!"
              shell: bash
